[package]
name = "simulate"
version = "0.1.0"
authors = ["John Simon <john@whatisaph.one>"]
edition = "2018"

[dependencies]
lazy_static = "1.1.0"

nalgebra = "0.18.0"
ncollide3d = "0.19.1"

rlbot = "0.4.1"

common = { path = "../common" }
oven = { path = "../oven" }

[features]
strict = []
